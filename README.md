# Centrum obchodních rizik ASTORIE

MVP 0.2 – interaktivní rizika: detail rizika, vlastní riziko, poznámky a pracovní klientský výstup.

Render:
- Build: `pip install -r requirements.txt`
- Start: `uvicorn aplikace.main:app --host 0.0.0.0 --port 10000`
