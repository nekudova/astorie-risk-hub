# Centrum obchodních rizik ASTORIE

MVP 0.3 – vstupní klientský dotazník, ARES načítání podle IČO, interaktivní rizika, vlastní riziko a pracovní klientský výstup.

Render Start Command:

```bash
uvicorn app.main:app --host 0.0.0.0 --port $PORT
```
