services:
  - type: web
    name: astorie-risk-hub
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn aplikace.main:app --host 0.0.0.0 --port $PORT
